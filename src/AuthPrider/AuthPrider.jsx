import { getAuth } from "firebase/auth";


const AuthPrider = () => {
    export const Auth
    const auth= getAuth(app);

    return (
        <div>
            
        </div>
    );
};

export default AuthPrider;